/** @brief execute the join command
 * la fonction est appelée si le resultat du parsing 
 * de join est valide
 * 
 * rajoute le client 
 * au channel (ajoute sa ocket et change son channel dans son instance)
 * 
 * @params:
 * - l'utilisateur qui veut join
 * - le chanel qu'il veut join
 * 
 * */